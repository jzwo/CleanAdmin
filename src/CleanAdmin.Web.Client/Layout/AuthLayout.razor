@inherits LayoutComponentBase
@inject IStringLocalizer<AuthLayout> L

<div class="min-h-screen flex items-center justify-center bg-body-bg">
    <div class="w-full max-w-md p-8 bg-bg-base rounded-lg shadow-lg">
        <div class="mb-8 flex items-center justify-between gap-4">
            <div class="flex-none">
                <CleanAdminLogo Class="h-16 w-16"/>
            </div>
            <div class="flex-1">
                <Title Level="2" Class="mb-0">@L["Clean Admin"]</Title>
            </div>
            <div class="flex flex-col items-end gap-2">
                <ThemeToggle/>
                <CultureSelector/>
            </div>
        </div>
        <div>
            @Body
        </div>
    </div>
</div>

@code
{
}
